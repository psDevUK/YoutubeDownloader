name: YoutubeDownloader 
on:
  push:
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Check module installs
        shell: pwsh
        run: Install-Module -Name YoutubeDownloader; Import-Module YoutubeDownloader
